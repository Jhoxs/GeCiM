{{>navbar}}

<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card text-center">
                <div class="card-body">
                    {{#if user}}

                    {{else}}
                    <h2>No se ha encotrado el usuario</h2>
                    {{/if}}
                </div>
            </div>
        </div>
    </div>
</div>
